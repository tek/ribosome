<<: !include extensions.yaml

dependencies:
  - name: base
    version: '>= 4 && < 5'
    mixin:
      hiding (Prelude)

ghc-options:
  - -Wall
  - -Wredundant-constraints
  - -Wincomplete-uni-patterns
  - -Wmissing-deriving-strategies
  - -Widentities
  - -Wunused-packages
